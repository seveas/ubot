* Port old ubot documentation
* Login page and overview page
* Document admin iface
* Control helpers from the web
* Bot tracks all helpers
  - Helpers identify at startup and when they see the bot
  - Bot tracks helpers (NameOwnerChange) and vice versa
* Make run_from_source.sh be careful when overwriting existing files
* More channel options (part, send message, nicks)
* Make run_from_source install datafiles
* Make run_from_source create the media symlink
* Port plugins
* Port ircstring to ruby and use it
* Find a way to do something similar in perl
